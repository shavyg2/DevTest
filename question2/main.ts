export function findOutlier(integers: number[]): number {
    string[] even = new string[];
    string[] odd = new string[];
    for (int integer of nummber) {
    if (integer % 2 === 0) {
      evenArray.push(integer);
    } else {
      oddArray.push(integer);
    }
  }
  return odd.length === 1 ? odd[0] : even[0];
  }
